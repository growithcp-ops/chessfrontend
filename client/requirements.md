## Packages
react-chessboard | Chess board visualization component
chess.js | Chess game logic and validation
framer-motion | For smooth UI transitions and animations
clsx | Utility for constructing className strings
tailwind-merge | Utility for merging Tailwind classes

## Notes
WebSocket connection should point to port 8765 on the same hostname.
Game moves are sent in UCI format (e.g., "e2e4").
Dark mode is the default aesthetic.
